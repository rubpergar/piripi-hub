# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

# GitHub recommends pinning actions to a commit SHA.
# To get a newer version, you will need to update the SHA.
# You can also reference a tag or branch,
#but the action may change without warning.

name: Build and Publish image in DockerHub

on:
  push:
    branches: 
     - docker
     - main
     - develop

jobs:
  build-and-push:
    name: Build and Push Docker image to DockerHub
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Docker Buildx
        uses: docker/set-up-buildx-action@v3

      - name: Build Docker image with tag
        run: |
          docker build -t piripi-hub:latest -f ./docker/images/Dockerfile.dev .  

      - name: Push Docker image to DockerHub
        run: |
          docker push piripi-hub:latest      

